@using EntityLayer.Concrete;
@model List<Feature2>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<br />
<h3>Öne Çıkanlar 2</h3>
<br />
@{
    int count = 0;
}
<table class="table table-hover">
    <tr>
        <th>#</th>
        <th>Başlık</th>
        <th>Açıklama</th>

        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        count++;

        <tr>
            <th>@count</th>
            <td><img src="@item.Image" style="height:50px;width:50px" /> @item.Title</td>
            <td>@item.Description</td>
            <td>

                <i type="button"
                   id="btnactions"
                   data-bs-toggle="dropdown"
                   aria-haspopup="true"
                   aria-expanded="false"> <i class="bx bx-dots-vertical-rounded"></i> </i>
                <div class="dropdown-menu dropdown-menu-end" aria-labelledby="btnactions">

                    <a asp-action="DeleteFeature" asp-route-id="@item.Feature2ID" class="dropdown-item btn btn-outline-danger"><i class='bx bxs-trash-alt'></i>Sil</a>

                    <a asp-action="UpdateFeature" asp-route-id="@item.Feature2ID" class="dropdown-item btn btn-outline-primary"><i class='bx bxs-edit-alt'></i>Düzenle</a>



                </div>

            </td>
        </tr>
    }
</table>
<br />
<a href="/Admin/Feature2/AddFeature" class="btn btn-dark"><i class='bx bxs-plus-circle'></i>Yeni Öne Çıkan Oluştur</a>


